@using Group4_ReadingComicWeb.Models.Enum
@model List<Report>

@{
    ViewData["Title"] = "User Reports";
    ViewData["Breadcrumb"] = "User Reports";
    Layout = "~/Views/Shared/_ModeratorLayout.cshtml";
}

@section Styles {
    <link rel="stylesheet" href="~/css/components/report-list.css" asp-append-version="true" />
}

<!-- Page Header -->
<div class="page-header">
    <div class="page-header-left">
        <h1>User Reports</h1>
        <p>Manage and review user reports submitted by community members.</p>
    </div>
</div>

<!-- Reports Table -->
<div class="data-card">
    <div class="data-card-header">
        <h3 class="data-card-title">Pending Reports</h3>
        <span class="badge report-count-badge">
            @Model.Count
        </span>
    </div>

    @if (Model.Any())
    {
        <div style="overflow-x: auto;">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Reporter</th>
                        <th>Target User</th>
                        <th>Type</th>
                        <th>Reason</th>
                        <th>Created Date</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var report in Model)
                    {
                        <tr>
                            <td class="report-reporter">@(report.Reporter?.Username ?? "Unknown")</td>
                            <td class="report-reporter">@(report.TargetUser?.Username ?? "Unknown")</td>
                            <td>
                                <span class="badge report-type-badge">
                                    @report.ReportType
                                </span>
                            </td>
                            <td>@report.Reason</td>
                            <td>@report.CreatedAt.ToString("MMM dd, yyyy")</td>
                            <td>
                                <a href="@Url.Action("Details", "Report", new { id = report.ReportId })"
                                   class="action-btn table-action-review">
                                    View
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state">
            <i class="ph ph-check-circle empty-state-icon"></i>
            <p class="empty-state-title">No pending reports!</p>
            <p class="empty-state-desc">All reports have been reviewed.</p>
        </div>
    }
</div>
